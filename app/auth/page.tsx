'use client'
import {useState} from 'react';import {createClient} from '@/lib/supabase/client';import {useRouter} from 'next/navigation'
export default function Auth(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const router=useRouter();async function login(){const s=createClient();const {error}=await s.auth.signInWithPassword({email,password});if(error)setMsg(error.message);else router.push('/app')}async function signup(){const s=createClient();const {error}=await s.auth.signUp({email,password});setMsg(error?error.message:'Cuenta creada. Si Supabase pide confirmación, revisa tu correo.')}return <main className="auth"><div className="eyebrow">Solo ustedes</div><h2 style={{fontSize:36,marginBottom:8}}>Bienvenido a Juntos 🤍</h2><p className="muted">Cada uno entra con su propia cuenta. Lo privado sigue siendo privado hasta que decidas compartirlo.</p><label className="field">Correo<input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="tu@correo.com"/></label><label className="field">Contraseña<input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="••••••••"/></label>{msg&&<p className="tiny">{msg}</p>}<div className="actions"><button className="btn" onClick={login}>Entrar</button><button className="btn secondary" onClick={signup}>Crear cuenta</button></div></main>}
